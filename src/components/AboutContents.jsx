import Link from 'next/link';
import React from 'react';

const AboutContents = () => {
    return (
        <div>
            <h1>it's also added the content page</h1>
            
        </div>
    );
};

export default AboutContents;